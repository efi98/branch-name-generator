export enum workItemTypes {
    Requirement = 'Requirement',
    Task = 'Task',
    Bug = 'Bug'
}

export enum snkeOsType {
    feature = 'feature',
    bugfix = 'bugfix',
    hotfix = 'hotfix',
    version = 'version'
}

export enum theme {
    light = 'light',
    dark = 'dark'
}

export enum mode {
    snkeOS = 'snkeOS',
    azureDevOps = 'azureDevOps'
}