import { theme } from "./enums";

export const DEFAULTS = {
    theme: theme.light,
    isSnkeOSMode: false,
    showModeSwitch: true,
    showWelcomeMsg: true,
    showSubmitAlert: true,
    showFormChangeAlert: true
};